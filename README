================================================================
EXECUTE CODE :
================================================================

g++ -Wall -o program main.cpp string.cpp

make

================================================================
USING GIT :
================================================================

git add <all modified files>

git commit -m "<nom commit>"

git pull //always pull before push and resolve conflicts

git push

git pull // should return "already updated" to prove push worked

================================================================
DATA MEMBERS : //all values in the class (not functions)
================================================================

char* data_ //list of characters
int size_ // size of list 
int max_size_ // value decided of list's max size (100)
int capacity_ // empty list cases (nb of free spaces = n-1 empty cases because of \0)



Implement those functions that fall to you:

    Student A (Nathanaël):
        copy constructor
        c_str()
        size()
        clear()
        operator=(char)
        operator+(const string&, const char*)
    Student B (Israël):
        constructor from a c-string
        length()
        max_size()
        resize(size_t, char)
        operator=(const string&)
        operator+(const string&, char)
    Student C (Axel):
        destructor
        capacity()
        empty()
        reserve(size_t)
        operator=(const char*)
        operator+(const string&, const string&)
        
================================================================
ALLOWED FUNCTIONS :
================================================================
#include <cstring>
strlen //size of string
strcpy //copy string
memset //allocate memory space
memcpy //copy memory space
